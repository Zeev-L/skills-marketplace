name: bulletize
description: Converts text into bullet points
commands:
  - name: bulletize
    description: Convert text to bullet points
    prompt_file: skill.md
    arguments:
      - name: input
        description: The text to convert
        required: true
